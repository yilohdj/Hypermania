namespace Scenes.Menus.CharacterSelect
{
    public enum SelectPhase
    {
        Character,
        Options,
        Confirmed,
    }

    public enum PlayerRole
    {
        Local,
        Remote,
    }
}
